function solve(arr) {
  // აქ შევინახავთ სწორად დალაგებულ მიმდევრობას
  let result = [];

  // 1) უნდა ვიპოვოთ საწყისი რიცხვი 
  for (let i = 0; i < arr.length; i++) {
    // ვვარაუდობთ რომ arr[i] არის საწყისი
    let isStart = true;

    // ვამოწმებთ ყველა სხვა რიცხვს
    for (let j = 0; j < arr.length; j++) {
      
    }
 }
}
