const numbers = [1, 2, 3, 4];

const doubled = numbers.map(num => num * 2);

console.log(doubled);


const names = ["nino", "gio", "luka"];

const upperNames = names.map(name => name.toUpperCase());

console.log(upperNames);

