const age = 16;
const hasTicket = true;

// ორივე უნდა იყოს true → AND ოპერატორი &&
if (age > 15 && hasTicket === true) {
  console.log("You can go in, and watch a movie");
} else {
  console.log("You can't go in");
}
