const myName = "Enriko";
const myAge = 15;

const userName = "Enriko";
const userAge = 15;

// ვამოწმებთ: ან სახელი უდრის ან ასაკი უდრის
if (myName === userName || myAge === userAge) {
  console.log("We have the same name or age");
} else {
  console.log("We don't have the same name and age");
}
